{
  "schema_version": "1.0",
  "policy_id": "kubernetes-digest-pin",
  "description": "Images must be digest-pinned; floating tags alone are rejected.",
  "required": [
    "image.digest matches sha256:[0-9a-f]{64}",
    "rendered Deployment image uses repository@digest form"
  ],
  "prohibited": [
    "tag-only image references without digest",
    "latest tag"
  ]
}
