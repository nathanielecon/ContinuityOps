# Secrets and local env
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
**/secrets/**

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Language / build
node_modules/
dist/
build/
coverage/
.turbo/
.next/
*.tsbuildinfo
__pycache__/
*.pyc
.venv/
venv/
target/
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
crash.log

# Agent / local runtime
.cursor/
# .codex/ holds change-controlled Codex Cloud Environment scripts (D-026) and is
# tracked. Only ever-local credential/cache/clutter material inside it is ignored.
.codex/auth.json
.codex/*.local
.codex/cache/
.codex-last-message-*
# Local stamp for scripts/Watch-CodexRefs.sh (never credentials)
.codex-warm/
*.log
tmp/
.cache/

# Cloud / kube local
kubeconfig
*.kubeconfig
.aws/
