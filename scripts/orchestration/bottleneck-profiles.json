{
  "schema_version": "1.0",
  "session_scope": "same_supervisory_session",
  "permission_expansion": false,
  "allowed_outputs": ["diagnosis", "handoff"],
  "profiles": [
    { "id": "auth", "scope": "诊断认证阻塞并返回交接", "may_mutate": false },
    { "id": "ci", "scope": "诊断托管 CI 阻塞并返回交接", "may_mutate": false },
    { "id": "toolchain", "scope": "诊断工具链阻塞并返回交接", "may_mutate": false }
  ]
}
