# Agentic remediation apply — protected human gate required.
name: agentic-remediate

on:
  workflow_dispatch:
    inputs:
      proposal_id:
        required: true
        type: string
      confirm:
        description: Type apply to confirm
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  apply:
    if: github.event.inputs.confirm == 'apply'
    runs-on: ubuntu-latest
    environment: agentic-apply
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Remediation apply stub
        run: |
          echo "Protected environment gate placeholder — no live mutation executed."
          echo "Proposal: ${{ github.event.inputs.proposal_id }}"
