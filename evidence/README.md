# Evidence

Append-only, commit-bound artifacts. Do not overwrite events; derive manifests
from the event log per `EVIDENCE_AND_CLAIMS.md`.

| Path | Purpose |
| --- | --- |
| `hosted/` | Hosted CI / delivery runs |
| `slices/` | Per-slice verification packs |
| `schema/` | Evidence schemas and adapters |
| `postbuild/` | Post-build partition certification |
| `judges/` | Saved and fresh council results |
