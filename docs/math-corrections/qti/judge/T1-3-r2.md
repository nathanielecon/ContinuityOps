SLICE: T1-3          SCORE: 10/10
ITEMS WORKED: 8 of 8     (none skipped; re-read from the raw XML after the fix, not from the slice cache)

VERDICT: both r1 defects are fixed, nothing regressed. Slice is clean.

WHAT THE FIXER CHANGED

  `topic_1_3_independent_practice_accuracy_check.xml`
    42683 bytes -> 42373 bytes, delta -310 bytes exactly.
    The escaped paragraph
    `&lt;p&gt;&lt;strong&gt;Canvas accuracy check:&lt;/strong&gt; Select every choice that is part of a correct solution. Select no incorrect choices.&lt;/p&gt;`
    is 155 bytes; 2 x 155 = 310. The byte delta is accounted for entirely by the
    two deletions, so no other byte in the file moved. Raw-file search for that
    escaped paragraph now returns 0 occurrences.
    `imsmanifest.xml` and `assessment_meta.xml` are unmodified (mtime and md5
    unchanged; manifest md5 ff188fe13d404b2290af380b455ee347, meta md5
    288525eff9ce4da6c023f48398491432).

VERIFICATION REQUESTED BY THE COORDINATOR

  1. Exactly one instruction block per stem — CONFIRMED.
     All 8 T1-3 stems contain the substring `Canvas accuracy check` exactly once.
     A corpus-wide re-sweep of all 157 items across the 14 packages now returns
     0 items with a doubled block (was 10 before this fix round: topic-1-2
     P1Q3/P2Q3, topic-1-3 P1Q1/P2Q1, topic-1-4 P1Q1-Q3/P2Q1-Q3 — the non-T1-3
     ones were evidently fixed by other slices' fixers in the same round).

  2. Surviving block byte-identical to the corpus canonical one — CONFIRMED,
     at both the parsed and the raw-byte level.
     Parsed: each of the 8 stems ends with exactly
     `<p><strong>Canvas accuracy check:</strong> Select every choice that belongs in complete correct answer. Part 1 and Part 2 both must be correct when prompt has multiple parts. Select no incorrect choices.</p>`
     which is the majority corpus form, now carried by 85 Shape A items.
     Raw: the escaped form of that block appears exactly 8 times in the T1-3
     file and is byte-for-byte the same string that appears 8 times in
     `topic_1_1_independent_practice_accuracy_check.xml`.
     Corpus-wide there are only 3 distinct trailing instruction blocks: the
     canonical one (85 items, including all 8 of mine), no block at all (the 69
     Shape B 6th-grade items, which use their own instruction), and a
     deliberate variant on 3 topic-sc-2 items that adds "including every
     equivalent form of that answer". None of the three variants is in my
     slice, and the topic-sc-2 wording is a pre-existing design choice for
     equivalent-form items, not a regression from this fix.

  3. Part A / Part B problem text above the block untouched — CONFIRMED.
     Each stem was compared against the exact problem-text prefix recorded from
     the pre-fix file in r1, and each is equal to `prefix + canonical block`
     with nothing else. Specifically, P1Q1 still reads
     `<p>In the first round of a board game, a player scores 10 points. On their next turn, they lose 4 points. On their last turn of the round, they lose 8 points.</p><p><strong>Part A:</strong> Explain how to show the change in score using a number line.</p><p><strong>Part B:</strong> What is the final score?</p>`
     and P2Q1 still reads
     `<p>In the first round of a card game, a player scores 12 points. On their next turn, they lose 5 points. On their last turn of the round, they lose 9 points.</p><p><strong>Part A:</strong> Explain how to show the change in score using a number line.</p><p><strong>Part B:</strong> What is the final score?</p>`
     Both `<strong>Part A:</strong>` and `<strong>Part B:</strong>` survive with
     their markup intact. No splice artifact: 0 occurrences of `</p>  <p>`,
     0 double spaces before `Select`, 0 empty `<p></p>`.

  4. Nothing else regressed — CONFIRMED on every axis I checked in r1.
     - File is well formed and parses; no BOM; XML declaration intact; the
       document still closes `</item></section></assessment></questestinterop>`.
     - File is pure ASCII (0 non-ASCII bytes), so no smart-quote or dash
       substitution crept in during the edit.
     - Counts: 8 `<item>`, 64 `<response_label>`, 72 `texttype="text/html"`
       (8 stems + 64 choices), 64 `<varequal>`, 50 `<not>`. 14 required + 50
       negated = 64, matching 8 items with 14 `correct_*` and 50 `wrong_*`.
     - All 64 choice texts are byte-identical to the values recorded in r1,
       in the same order, with the same idents. No choice text, value, sign or
       dollar amount changed.
     - Every item still has exactly one `<respcondition>` with
       `<setvar action="Set">100</setvar>`; every `correct_*` required by a bare
       `<varequal>`; every `wrong_*` inside `<not>`; `respident="response1"` on
       all 64 varequals. Required set equals the `correct_*` ident set and
       negated set equals the `wrong_*` ident set on all 8 items (tree parsed
       with `<not>` resolved, not regex-matched).
     - No two choices in any item share visible text (8 distinct texts per item).
     - Math delimiters still balanced and consistent: `\(-12 + 19 =\)` and
       `\(-8 + 14 =\)` are the only math spans, both `\(...\)`; no `$...$` math
       delimiter anywhere. Currency `$` characters are unchanged and plain.
     - Still no images and no `src=` attributes, so nothing to resolve against
       `$IMS-CC-FILEBASE$`.
     - `points_possible` 8.0 still matches 8 items.

MATHEMATICS RE-CONFIRMED (unchanged from r1, since all 64 choices and all 8
scoring keys are byte-identical to the pre-fix file)

  P1Q1  10 + (-4) + (-8) = -2; after two turns 10-4 = 6. Keyed: the number-line
        description, the expression, 6, -2. Non-keyed `10 - (-4) + (-8)` = 6,
        `at 14`, `are 2`, `right 10, right 4` all false.
  P1Q2  15 + (-20) + 3 = -2. Keyed -2; the other seven differ.
  P1Q3  6 x 12.50 = 75; 500 - 75 = 425. Keyed $425; the other seven differ.
  P1Q4  -12 + 19 = 7. Keyed 7; the other seven differ.
  P2Q1  12 + (-5) + (-9) = -2; after two moves 12-5 = 7. Keyed: description,
        expression, 7, -2. Non-keyed `12 - (-5) + (-9)` = 8, `at 17`, `are 2`,
        `right 12, right 5` all false.
  P2Q2  18 + 4 + (-25) = -3. Keyed -3; the other seven differ.
  P2Q3  8 x 8.75 = 70; 400 - 70 = 330. Keyed $330; the other seven differ.
  P2Q4  -8 + 14 = 6. Keyed 6; the other seven differ.

  Criterion 2 (true-but-unkeyed) clean: no distractor in this slice is an
  equivalent form, unsimplified form, alternative valid expression, restated
  value, or right value in different units. Criterion 3 (false-but-keyed) clean.
  Criterion 4: keyed sets reproduce both answer-key PDFs' Lesson 1-3 answers
  (Part 1: -2 with the right-10/left-4/left-8 moves, -2, $425, 7; Part 2: -2
  with the right-12/left-5/left-9 moves, -3, $330, 6), and neither key offers an
  "or" alternative, so all-or-nothing keying excludes no acceptable form.
  Criterion 1: stems still match the rendered assignment page 2 on every
  quantity. Criterion 5: every keyed choice remains derivable from the
  assignment alone.

NOTHING STILL STANDS

  Both r1 defects are closed. No new defect. Score 10/10.

STALE-ARTIFACT NOTE (not a defect in the package, flagged for the coordinator)

  The slice cache `/tmp/qtiwork/slices/T1-3.json` still carries
  `"zip_sha12": "8508e8f4dfb8"` and the pre-fix doubled-instruction stem text
  for P1Q1 and P2Q1. It was built before the fix and is now stale. Anyone
  re-scoring from the cache rather than the raw XML will see the old stems and
  may re-report the fixed defect. Recommend regenerating the cache, or
  instructing the cold validation judge to read the XML directly for stem text.
