SLICE: T1-1          SCORE: 10/10
SHAPE: A (Topic 1, multiple_answers_question, all-or-nothing)
ITEMS WORKED: 8 of 8     (none skipped; all 8 re-worked from the post-fix XML)
ROUND: r2, re-score after the fixer applied the four r1 fixes

WHAT CHANGED, VERIFIED INDEPENDENTLY

  Package XML md5 now 7f0e265464039ca13110f679acf7b337 (was
  10e0265a5295ee7324e876edb7f66bfa at r1). To prove the fixer changed only what
  it claimed, I reverted the four replacement strings in memory and re-hashed:
  the result is 10e0265a5295ee7324e876edb7f66bfa, byte-identical to the r1
  original, net delta +3 bytes. Each of the four search strings occurs exactly
  once. So no other byte of the file moved - no stem, no ident, no scoring node,
  no metadata. imsmanifest.xml and assessment_meta.xml are untouched (mtimes
  still 2026-01-01; assessment_meta md5 300687bf3028ec3d691938e62613fa30,
  unchanged from r1). File is still 845 lines and still parses as well-formed
  XML.

EACH REPLACEMENT RE-WORKED - IS THE NEW TEXT GENUINELY FALSE?

  1_1_part_1_question_1_wrong_1  now "+18 and +18"
    Stem: submarine rises 18 m, then descends 18 m; Part A asks for both changes
    as signed numbers. Rise = +18, descent = -18. The choice asserts the second
    change is +18, i.e. that the submarine rose twice; their sum would be +36,
    not 0. FALSE, and false on the one feature the item is testing (sign of a
    descent), so it is a distractor that teaches rather than traps.
    The ± ambiguity is gone: no "+-" remains anywhere in the package.
    Checked for the two ways this fix could have gone wrong and neither
    happened: it is not "18 and -18" (bare 18 would read as +18 and be TRUE),
    and it is not "-18 and +18" (the same two signed numbers in the other order,
    which a student could defend as an unordered pair and which would therefore
    be arguably true). Distinct from all seven other choice texts in the item.

  1_1_part_1_question_2_wrong_4  now "|a| = 2|b|"
    Constraints: a < 0, b > 0, a + b = 0, so a = -b and |a| = |b| = b > 0.
    Suppose |a| = 2|b|. Then b = 2b, so b = 0, contradicting b > 0. FALSE, and
    false for every pair satisfying the stem, not merely for typical values.
    It is an equation about absolute values, so it sits in the same category as
    the keyed answer and reads as a plausible distractor. It is not equivalent
    to any keyed statement and not implied by the stem. (It does entail
    wrong_6 "|a| != |b|"; both are false, so no student can be punished by the
    overlap.) Distinct visible text from all seven others.
    The r1 defect is genuinely closed: "a + b = 0" no longer appears as a choice
    anywhere, so the stem-given, key-equivalent truth is no longer negated.

  1_1_part_2_question_2_wrong_4  now "|p| = 2|q|"
    Constraints: p > 0, q < 0, p + q = 0, so q = -p and |p| = |q| = p > 0.
    Suppose |p| = 2|q|. Then p = 2p, so p = 0, contradicting p > 0. FALSE.
    Same reasoning as Part 1, mirrored; "p + q = 0" no longer appears as a
    choice. Distinct visible text from all seven others.

  1_1_part_2_question_4 stem  now "Order from least to greatest: \(6,\ -2,\ 4\)"
    Delimiters now match P1Q4's "\(-3,\ 8,\ -1\)" and the rest of the package.
    Package-wide count: 18 "\(" and 18 "\)", balanced, and 0 "$" characters
    anywhere in the file - the expected total is 18 (7 in each of the two Q2
    stems, 1 each in P1Q3, P2Q3, P1Q4, P2Q4), so exactly one pair was added and
    nothing else was disturbed. Numbers still match the assignment ("Q4. Order
    from least to greatest: 6, -2, 4") and the key ("Answer: -2, 4, 6").

NO DEFECT MOVED, NO NEW DEFECT INTRODUCED

  Criterion 2 re-sweep of every non-keyed choice in all 8 items, post-fix:
  P1Q1: +18 and +18 (false, above); +19/+17/+20/+16 with -18 - wrong first
        addend, all false.
  P1Q2: |a| > |b| false and |a| < |b| false (they are equal); a = b false
        (a < 0 < b); |a| = 2|b| false (above); |a| + |b| = 0 false (equals
        2|a| > 0); |a| != |b| false; "sign reversed" is a fragment, not an
        equation, and the item asks for an equation - carried over from r1 as a
        recorded weak case, ruled not a defect.
  P1Q3: -9, 10, 8, 11, 7, 0, 90 - none equals |-9| = 9.
  P1Q4: 3,-1,8 / -2,-1,8 / -4,-1,8 / -1,-1,8 / -5,-1,8 / 0,-1,8 / -30,-1,8 -
        none is the multiset {-3, -1, 8}.
  P2Q1: 24 and +24 (bare 24 means +24, but the first change is a descent, so
        false); -23/-25/-22/-26 with +24 - all false.
  P2Q2: mirror of P1Q2, same rulings.
  P2Q3: -11, 12, 10, 13, 9, 0, 110 - none equals |-11| = 11.
  P2Q4: 2,4,6 / -1,4,6 / -3,4,6 / 0,4,6 / -4,4,6 / -20,4,6 / 5,4,6 - none is
        the multiset {-2, 4, 6}.
  No true-but-unkeyed choice survives anywhere in the slice.

  Criterion 3, keyed choices re-worked: P1Q1 +18 / -18, sum 0, equal magnitude
  and opposite signs; P1Q2 |a| = |b|; P1Q3 9; P1Q4 -3, -1, 8; P2Q1 -24 / +24,
  sum 0, equal magnitude and opposite signs; P2Q2 |p| = |q|; P2Q3 11;
  P2Q4 -2, 4, 6. All true. None was touched by the fixer.

  Criterion 1, stem fidelity: seven stems are byte-identical to r1, where they
  were checked against the rendered Lesson 1-1 grid; the eighth (P2Q4) changed
  only its math delimiters and still carries 6, -2, 4.

  Criterion 4, key agreement: unchanged keyed sets still reproduce Part 1 and
  Part 2 keys for Lesson 1-1 Q1-Q4. The "(or equivalent)" allowance on each Q2
  is no longer contradicted, because the equivalent form that was being negated
  has been removed from the choice list.

  Criterion 5, answerability: unaffected; every keyed set is still producible
  from the assignment alone.

  Criterion 6, structure, re-counted mechanically from the parsed respcondition
  tree with not-blocks separated, for all 8 items: exactly one respcondition
  each (8 total), decvar maxvalue="100", respident="response1", 8
  response_labels each (64 total), required set == the correct_* set exactly,
  negated set == the wrong_* set exactly, and original_answer_ids matches the
  render_choice order ident-for-ident. No two choices within any item share
  visible text - checked over all 64 choices after stripping tags and
  unescaping, including the three new strings.

  Criterion 7, markup: 0 "$" delimiters, balanced \( and \), no img tags, no
  media/ references, so no $IMS-CC-FILEBASE$ exposure; manifest resources and
  their hrefs resolve to files present; the "Canvas accuracy check" paragraph
  appears exactly once per stem. The pre-existing cosmetic asymmetry noted at r1
  (comparison operators escaped with one layer in stems, two in choices) is
  unchanged and still renders correctly in both places; it was not scored at r1
  and is not scored now.

CANDIDATES / r1 DEFECTS - FINAL STATUS

  P1Q1 wrong_1 "+-18 and -18"      -> CLOSED. Now "+18 and +18", false, no ±.
  P1Q2 wrong_4 "a + b = 0"         -> CLOSED. Now "|a| = 2|b|", false.
  P2Q2 wrong_4 "p + q = 0"         -> CLOSED. Now "|p| = 2|q|", false.
  P2Q4 stem "$6,\ -2,\ 4$"         -> CLOSED. Now "\(6,\ -2,\ 4\)".
  No defect stands.

OPERATIONAL NOTE (not a package defect, no action taken by me)

  The slice cache /tmp/qtiwork/slices/T1-1.json still holds the pre-fix choice
  texts ("+-18 and -18", "a + b = 0", "p + q = 0") and the pre-fix P2Q4 stem,
  and its stem fields truncate at the "<" characters in the two Q2 stems. It is
  now stale. Anyone re-scoring T1-1 - including the cold validation judge -
  must read the package XML, not this cache, or they will re-raise the four
  closed defects. I did not modify it, per the brief.

CLEAN

  Part 1 Question 1
  Part 1 Question 2
  Part 1 Question 3
  Part 1 Question 4
  Part 2 Question 1
  Part 2 Question 2
  Part 2 Question 3
  Part 2 Question 4
