SLICE: T1-9          SCORE: 10/10     (round 2, re-scored after fix)
ITEMS WORKED: 4 of 4     (none skipped)

Shape A confirmed: 4 `multiple_answers_question` items, `respident="response1"`,
`correct_*` / `wrong_*` idents, exactly one `<respcondition>` per item with
`maxvalue="100"`, 8 choices per item.

All source checks in this round were run against the REPO copies:
  /home/user/ContinuityOps/docs/math-corrections/pdf/CorrectionsReport.pdf
  /home/user/ContinuityOps/docs/math-corrections/pdf/Topic1ExplanationsPart1.pdf
  /home/user/ContinuityOps/docs/math-corrections/pdf/Topic1Part1Solutions.pdf
  /home/user/ContinuityOps/docs/math-corrections/pdf/Topic1Part2Solutions.pdf
  /home/user/ContinuityOps/docs/math-corrections/source/Topic1IndependentPractice.pdf
  /home/user/ContinuityOps/docs/math-corrections/tex/parts/t1e_E.tex

DEFECTS
  (none outstanding)

DEFECT FROM ROUND 1, NOW RESOLVED -- VERIFIED APPLIED
  Part 1 Question 1 and Part 2 Question 1 | criterion 1 | stem, first sentence
    found (pre-fix, preserved in /tmp/qtiwork/slices/T1-9.json):
      "A business owner loses $450.75 as a result of a shipping delay. The 3
       owners of the business have to share the loss equally."
      "A business owner loses $625.50 as a result of damaged inventory. The 5
       owners of the business have to share the loss equally."
    why: the first sentence attributes the whole loss to one owner while the next
      says three (or five) owners share it. The assignment reads "A business loses
      $450.75 due to shipping delay" / "A business loses $625.50 due to damaged
      inventory". CorrectionsReport.pdf logs this exact sentence -- "p. 35 Low
      inconsistency / Where: Topic 1-9 Question 1 > Question block / Found: A
      business owner loses $450.75 ... / Corrected to: A business loses $450.75
      ... / Why: The first sentence attributes the whole loss to one owner, then
      the next sentence says three owners share it; the loss belongs to the
      business, not to a single owner." The correction was applied to the
      companion (tex/parts/t1e_E.tex line 10 reads "A business loses \$450.75 as a
      result of a shipping delay", and the rebuilt repo PDF renders it at line
      1580), so before the fix the QTI and the companion a student reads beside it
      disagreed on the first line of the same question.
    fix (applied by the coordinator, verified by me): the word "owner" deleted
      from both Q1 stems. Current text: "A business loses $450.75 as a result of a
      shipping delay. The 3 owners of the business have to share the loss
      equally." and "A business loses $625.50 as a result of damaged inventory.
      The 5 owners of the business have to share the loss equally."

  Verification that the fix is correct and correctly scoped:
   - "A business owner" now occurs 0 times package-wide; "A business loses" occurs
     twice, both in the item XML.
   - Re-parsed the item XML with `<not>` blocks stripped and diffed every field
     against the pre-fix slice cache: choice idents and visible text identical in
     all four items; `required` identical; `negated` identical; one
     `<respcondition>` per item; `decvar maxvalue="100"` on all four. The only
     content delta anywhere is the two deleted words.
   - `imsmanifest.xml` and `assessment_meta.xml` are untouched (original mtimes;
     points_possible still 4.0, both resource hrefs still resolve). Only
     `topic_1_9_independent_practice_accuracy_check.xml` was rewritten.
   - The XML still parses (ElementTree, no error), so import validity is intact.
   - Part 2 Q1 was corrected too even though CorrectionsReport logs only the Part 1
     occurrence -- there is no Part 2 explanations companion for it to have been
     logged against. Correcting it is right: the assignment's Part 2 Q1 reads "A
     business loses $625.50 due to damaged inventory" on the rendered page 3.

  Independent sweep for any OTHER un-applied correction in this package:
    Extracted all 111 "Found:" blocks from CorrectionsReport.pdf and tested each
    against the package's tag-stripped text by longest-prefix literal match
    (>=5 words). Result: 0 hits -- no logged-and-corrected string survives in the
    package. Instrument self-tested first against the PRE-FIX stem text preserved
    in the slice cache: it fires there, returning exactly the 24-word hit "A
    business owner loses $450.75 as a result of a shipping delay. The 3 owners of
    the business have to share the loss equally." So the scan detects the class of
    defect it is meant to detect, and its zero on the current package is a real
    zero rather than a broken probe.

WHERE MY ROUND-1 REASONING FAILED
  My r1 candidate D cleared this stem by matching it verbatim against
  `Topic1ExplanationsPart1.pdf` and concluding the wording was canonical. I read
  the scratchpad copy (308,307 bytes, 10:02) -- the uncorrected source the
  corrections pass was run AGAINST. The live companion is the repo copy
  (356,001 bytes, 15:28), rebuilt from corrected TeX, and it says the opposite.
  I matched the wording that had been corrected away from, and then reinforced the
  wrong conclusion with a house-style argument (topic-1-10 paraphrases the
  assignment too), which cannot clear a stem that has a published correction
  against it. Two method errors to carry forward:
   1. When two copies of a source exist, establish which one the corrections were
      applied to before citing either. Byte size and mtime distinguish them.
   2. Check the corrections log directly rather than inferring intent from a
      derived document. CorrectionsReport.pdf is the governing record; consulting
      it first would have surfaced this in one grep.
  Note that the same trap did NOT reach my key agreement: I read both Solutions
  PDFs from the repo path in r1, and their scratchpad twins are different files
  (md5 ef9e63../2e9185.. repo vs ead4ec../780fd1.. scratchpad). The assignment
  itself is not affected -- the scratchpad copy is byte-identical to
  docs/math-corrections/source/Topic1IndependentPractice.pdf (md5 c6e63e05..), so
  the page-3 render I based the mixed numbers on remains valid.

MATHEMATICS -- RE-CONFIRMED, UNCHANGED BY THE FIX
  Recomputed in exact rational arithmetic, against the repo answer keys:
    P1Q1: (-450.75) / 3 = -601/4 = -150.25   (check 3 x 150.25 = 450.75)
          repo key: "Answer: -150.25 (or -$150.25 per owner)"
    P1Q2: (31/2) / (31/8) = 31/2 x 8/31 = 4  -> key "4 water stations"
    P2Q1: (-625.50) / 5 = -1251/10 = -125.10 (check 5 x 125.10 = 625.50)
          repo key: "Answer: -125.10 (or -$125.10 per owner)"
    P2Q2: (33/2) / (11/4) = 33/2 x 4/11 = 132/22 = 6 -> key "6 rest stops"
  Both key parentheticals are single alternate spellings of one value and only one
  form is offered as a choice, so criterion 4's both-forms-required trap does not
  arise. (The repo keys already carry the "$ - 150.25" -> "-$150.25" notation
  correction logged at CorrectionsReport p.5 / p.4; the QTI choices write
  "-150.25 per owner" and "-125.10 per owner" and are unaffected.)

CANDIDATES RULED ON
  Round-1 audit claim "all 4 items clean, no equivalent-form traps"
  -> CONFIRMED as to arithmetic, REFUTED as to completeness: the arithmetic is
     right on all four items, but the audit missed the criterion-1 stem defect.

  Cold judge's criterion-1 overturn -> CONFIRMED. Accepted in full; my r1 clearance
     of it was wrong for the reason set out above.

  Mixed-number equivalent-form trap (improper / mixed / unsimplified) -> REFUTED.
     Both division items resolve to integers (4 and 6), so no improper-vs-mixed
     pair can exist. Both money items resolve to terminating two-place decimals and
     the forms that would be defects (-150 1/4, -$150.25, -150.250; -125.1,
     -125.100, -$125.10) appear in no distractor. Distractor values are pairwise
     distinct from the key in every item:
       P1Q1 -150.25 vs {150.25, -149.25, -151.25, -148.25, -152.25, 0.00, -15025}
       P2Q1 -125.10 vs {125.10, -124.10, -126.10, -123.10, -127.10, 0.00, -12510}
       P1Q2 4 vs {3, 5, 8, -4, 6, 2}   P2Q2 6 vs {5, 7, 12, -6, 8, 4}

  "Right value, wrong unit": P1Q2 `..._wrong_3` "4 rest stops" and P2Q2
  `..._wrong_3` "6 water stations" -> REFUTED as criterion-2 defects.
     Label swaps, not unit conversions of one quantity. Checked against BOTH parts,
     not just the item's own stem: rest stops exist only in Part 2 where the count
     is 6, so "4 rest stops" is false under either reading; water stations exist
     only in Part 1 where the count is 4, so "6 water stations" is false under
     either reading. Neither is true anywhere in the corpus.

  Weaker case, recorded explicitly per rubric: P1Q1 `..._wrong_1` "150.25 per
  owner" and P2Q1 `..._wrong_1` "125.10 per owner" are true arithmetic statements
  answering a different question (the magnitude of each share) -> REFUTED as
  defects. The stem asks for "the quotient that represents each person's share of
  the loss" in a lesson titled Divide Rational Numbers; both keys model the loss as
  negative, and the repo companion states the modelling step outright ("Write the
  loss as -450.75", "Three equal shares of a loss stay negative"). The unsigned
  value is the canonical sign-error distractor, not an answer to the question asked.

CLEAN
  Part 1 Question 1 -- stem now matches the corrected wording and the assignment's
    numbers ($450.75, 3 owners, assignment page 3 rendered at 160 dpi). Key
    "-150.25 per owner" reproduces the repo Part 1 key. All 7 distractors
    recomputed false. 8 choices, no duplicate visible text, one instruction block,
    exactly one literal "$" and no \( \) in this stem so no delimiter pair can
    form, no `src=`.

  Part 1 Question 2 -- "\(15\dfrac{1}{2}\)" and "\(3\dfrac{7}{8}\)" verified by
    rendering the assignment page (pdftotext -layout transposes these to "15 12"
    and "3 78", so the render governs). 15 1/2 / 3 7/8 = 4 exactly and the fourth
    interval endpoint IS the finish line (3 7/8, 7 3/4, 11 5/8, 15 1/2), so both
    readings of "and one at the finish line" give 4 and "5 water stations" is not
    defensibly true. Repo companion corroborates: "Final answer: 4 water stations",
    "Four intervals of 3 7/8 fit exactly into 15 1/2". Stem wording matches the
    repo companion's Q2 question block verbatim and carries no logged correction.
    All 7 distractors false. 2 `\(` / 2 `\)`, no "$".

  Part 2 Question 1 -- stem now matches the corrected wording and the assignment's
    numbers ($625.50, 5 owners). Key "-125.10 per owner" reproduces the repo Part 2
    key. All 7 distractors recomputed false. 8 choices, no duplicates, one
    instruction block, one literal "$".

  Part 2 Question 2 -- "\(16\dfrac{1}{2}\)" and "\(2\dfrac{3}{4}\)" verified by
    render. 33/2 / 11/4 = 6 exactly; stops at 2 3/4, 5 1/2, 8 1/4, 11, 13 3/4,
    16 1/2, the last being the finish line, so both readings give 6 and "7 rest
    stops" is not defensibly true. Key "6 rest stops" matches the repo Part 2 key.
    All 7 distractors false. Delimiters balanced.

  Package-level -- `imsmanifest.xml` declares the assessment resource plus the
    `assessment_meta.xml` dependency and both hrefs resolve to files present; no
    media directory and no `src=` anywhere, so the `$IMS-CC-FILEBASE$` image trap
    does not arise. `assessment_meta.xml` points_possible 4.0 matches 4 items at
    1 point. Every `correct_*` is required by a bare `<varequal>` and every
    `wrong_*` is negated by `<not><varequal>` in all four items; `<not>` blocks
    were stripped before reading the key, so the regex-catches-negated-varequal
    trap is avoided.

NON-SCORING OBSERVATION (working file, not the artifact under score)
  /tmp/qtiwork/slices/T1-9.json still carries the pre-fix stems for the two Q1
  items ("A business owner loses ..."). Its choice, ident, required and negated
  fields remain accurate. Any downstream consumer that reads stems from the cache
  rather than the package will see the uncorrected wording. This does not bear on
  the package's score; flagged so the next reader does not re-derive the defect
  from the cache or, worse, cite the cache as evidence the fix did not land.
