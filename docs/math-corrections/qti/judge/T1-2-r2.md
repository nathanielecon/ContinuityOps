# T1-2 judge report (round 2, post-fix re-score)

SLICE: T1-2 (topic-1-2-independent-practice-accuracy-check-qti)   SCORE: 10/10
ITEMS WORKED: 6 of 6     (none skipped)

All four r1 defects are repaired. Nothing further stands. Mathematics is
byte-identical to the pre-fix package, so the r1 mathematical verdict carries
unchanged and was re-confirmed against the answer keys.

## THE FOUR FIXES, VERIFIED

1. **P1Q3 `$...$` -> `\(...\)`** — CLEARED. Raw line 244 now reads
   `Jordan says \(0.\overline{27}\) is rational because its digits repeat.`
   Zero `$` characters remain anywhere in either Q3 stem.
2. **P2Q3 interleaved span** — CLEARED. Raw line 555 now reads
   `Mia says \(0.\overline{6}\) is rational because it equals \(\dfrac{2}{3}\). Lee says ...`
   Two properly nested `\(...\)` pairs, `$` gone.
3. **P1Q3 duplicated instruction block** — CLEARED. One block remains.
4. **P2Q3 duplicated instruction block** — CLEARED. One block remains.

## DELIMITERS: ORDERED, NOT MERELY COUNTED

The coordinator's caution is the right one — a naive `count('\(') == count('\)')`
returned **True** on the pre-fix P2Q3 string, which is exactly why r1 flagged it
by reading the span rather than counting it. So I re-checked with a positional
scanner: tokenise every `\(`, `\)` and `$` in document order, require strict
alternation starting with `\(`, depth never exceeding 1, depth 0 at end, and no
bare `$` at all.

**Instrument self-test first** (a scanner that passes everything proves nothing):

| input | scanner verdict | required |
|---|---|---|
| pre-fix P1Q3 `$0.\overline{27}$ ...` | False | False |
| pre-fix P2Q3 `$0.\overline{6}\( ... \)\dfrac{2}{3}$` | False | False |
| control `x \(a\) y \(b\) z` | True | True |

The scanner rejects both pre-fix strings and accepts the control, so it is
strictly stronger than the naive count that was fooled before. Post-fix result,
all six stems:

| item | token sequence | ordered | bare `$` |
|---|---|---|---|
| Part 1 Question 1 | (none) | yes | 0 |
| Part 1 Question 2 | `\( \) \( \)` | yes | 0 |
| Part 1 Question 3 | `\( \)` | yes | 0 |
| Part 2 Question 1 | (none) | yes | 0 |
| Part 2 Question 2 | `\( \)` | yes | 0 |
| Part 2 Question 3 | `\( \) \( \)` | yes | 0 |

LaTeX brace balance re-checked independently on every stem: `{` count equals
`}` count on all six, so `\overline{27}`, `\overline{6}`, `\dfrac{2}{3}`,
`\dfrac{x}{y}` and `\dfrac{a}{b}` are intact.

## CANONICAL INSTRUCTION BLOCK: INTACT, EXACT

Each of the six stems now contains exactly one occurrence of
`Canvas accuracy check`, and in each the stem **ends with** a byte-exact match of
the canonical block:

`<p><strong>Canvas accuracy check:</strong> Select every choice that belongs in complete correct answer. Part 1 and Part 2 both must be correct when prompt has multiple parts. Select no incorrect choices.</p>`

The string `part of a correct solution` — the non-canonical block r1 flagged —
occurs zero times in the package. The fixer deleted the stray block and kept the
canonical one, not the reverse; no wording was paraphrased, truncated, or merged.
Escaping is sound: the edited `mattext` bodies are singly HTML-escaped
(`&lt;p&gt;`, `&lt;strong&gt;`), zero occurrences of a double-escaped `amp;lt;`.

Corpus check as a side observation (outside this slice): the count of items
carrying a duplicated instruction block is now **0** package-wide, down from the
10 r1 measured, so T1-3 and T1-4 were repaired in the same pass.

## NO MATHEMATICS MOVED

Every item was re-parsed and compared field-by-field against
`/tmp/qtiwork/slices/T1-2.json`, which was built from the **pre-fix** package and
therefore serves as the before-image:

| item | choice idents + visible text identical to pre-fix | required/negated identical | structure |
|---|---|---|---|
| Part 1 Question 1 | yes | yes | ok |
| Part 1 Question 2 | yes | yes | ok |
| Part 1 Question 3 | yes | yes | ok |
| Part 2 Question 1 | yes | yes | ok |
| Part 2 Question 2 | yes | yes | ok |
| Part 2 Question 3 | yes | yes | ok |

"Identical" here is exact string equality on the ordered list of eight
`response_label` idents and on the ordered list of their stripped visible texts,
plus exact equality on the ordered `required` and `negated` ident lists. Not one
choice, key, ident or numeral changed. The edit touched only the two Q3
`<mattext>` stem strings.

Structure re-verified independently rather than assumed: all six items still have
exactly one `<respcondition>`, `maxvalue="100"` / `minvalue="0"`, `setvar 100`,
`respident="response1"` throughout, 8 choices, no two choices sharing visible
text, every `correct_*` bare-required and every `wrong_*` `<not>`-negated. The
file re-parses as well-formed XML. `imsmanifest.xml` and `assessment_meta.xml`
were not touched (`points_possible` still 6.0 in both places, resource and
dependency entries unchanged).

The r1 mathematics therefore stands verbatim and needs no re-derivation, but the
load-bearing values were re-checked against the keys: 3/8 = 0.375 and
5/8 = 0.625 (Part 1 / Part 2 `Answer:` lines); Part 1 Q2 keys the assignment's
option C and Part 2 Q2 keys option B, matching `Answer: C` and `Answer: B`;
P1Q3 keys {Jordan, 3/11, 99x = 27} against `Answer: Jordan; 0.27bar = 3/11`;
P2Q3 keys {Mia, 2/3, 3x = 2} against `Answer: Mia; 0.6bar = 2/3`. The two
weaker cases r1 recorded and ruled on — `The sign does not matter here.` in both
Q2 items, and P2Q3 keying `3x = 2` where the key derives `9x = 6` — are
unaffected by a stem-only edit and remain ruled not defects. In particular the
fixer correctly left `3x = 2` alone, as r1 asked.

## NOTHING STANDS

No defect remains under any of the seven criteria. Criterion 7 was the only one
r1 scored against and it is now satisfied on all six items; criteria 1-6 were
clean in r1 and are re-confirmed unchanged.

## CLEAN

- Part 1 Question 1
- Part 1 Question 2
- Part 1 Question 3
- Part 2 Question 1
- Part 2 Question 2
- Part 2 Question 3
