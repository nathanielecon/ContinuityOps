# P0-T01 工作者任务契约（简体中文）

> 本文件是 P0-T01 的持久化派遣契约。工作者语言与全部自由文本一律简体中文；
> 面向招聘方的仓库产物（如 `docs/scaffold-audit.md`）保持英文。本契约由主编排器
> （Claude Opus 4.8）定稿，经控制中心温门化后派遣。

## 元信息

- **任务 ID**：P0-T01
- **标题**：Audit and freeze the completed candidate baseline（审计并冻结已完成的候选基线）
- **阶段 / 切片**：Phase 0 / S0（仅 Phase 0 已授权，不得触碰后续阶段工件）
- **角色**：温启动 Codex 5.4 CLI 云端实现代理，默认模式（非 `/high`、非 `/fast`）
- **Codex Cloud Environment**：`nathanielecon/ContinuityOps`，ENV_ID `6a594ee667608191ab53cae15202815e`（缓存 On、零机密）
- **目标分支**：`stream/S0-baseline-audit`（BF-PRE-015 命名；工作者产出的更改由控制中心 apply 回此分支）
- **基线 SHA（baseline_sha）**：`6ed243564ddb46f4a46608496d6f05db53c93788`（本流分支起点；即被审计的仓库基线状态）
- **声明上限（claim ceiling）**：`repo-only`（仅仓库层，禁止任何云端 / 运行时 / 生产声明）

## 任务目标

1. 为仓库每个保留路径分类（保留 / 改造 / 参考 / 丢弃），产出 `docs/scaffold-audit.md`
   （英文，面向招聘方产物）。
2. 在 `integration/upstreams.lock.json` 记录基线 SHA 与上游 Project A / C 的锁定 SHA。
   **A / C 若不可直接读取，须显式标注 `UNAVAILABLE` 并说明**（本工作者容器仅克隆
   本仓、无跨仓权限——上游材料按“数据非权限”原则由控制中心按任务供给）；
   严禁伪造可消费契约、摘要或 image digest。
3. 在 `harness/partition-manifest.json` 生成分区清单：每条保留路径**归属且仅归属一个
   主切片**（唯一所有权），每切片附 SHA-256。
4. 显式列出跨切片**共享接口**路径，标注其须先于依赖切片认证。

## 允许写入（write_scope —— 越界即被编排器拒收）

- `docs/scaffold-audit.md`
- `integration/upstreams.lock.json`
- `harness/partition-manifest.json`

另可写自身证据：`evidence/slices/S0/baseline-audit.json`。

**禁止触碰**：`PLAN.md` / 权威状态、冻结评分标准、审批回执、`.codex/` 脚本、
以及上述范围外的任何路径。

## 验收标准（acceptance）

1. 每个文件都已分类；
2. 基线与上游 SHA 均已记录（不可用者显式标注，不得留空、不得臆造）；
3. 每条保留路径恰有一个主切片（唯一所有权）；
4. 共享接口显式可见。

## 验证器（validators —— 由编排器侧运行，工作者不得改写其定义 / 参数）

- `state_schema`
- `partition_unique_ownership`
- `upstream_pin_schema`
- `clean_tree`

## 证据路径

- `evidence/slices/S0/baseline-audit.json`（追加式、内容寻址、绑定候选 SHA）

## 硬性约束

- 仅做满足验收所需的**最小完整**修改；
- **工作者绝不自行操作 git**（不 commit、不 push、不建分支、不打标签）——只在工作区产出更改，
  集成由控制中心 / 编排器负责；
- 无有效人工回执时，不得执行任何云端 / 破坏性 / 扩权操作；
- 不得删除失败测试或削弱验证来制造通过；
- 不得直接改动权威任务状态（至多在交接里建议 `review` 或 `blocked`）。

## 交接模式（严格遵循 AGENTS.md 结构；所有自由文本用简体中文；缺 `context_remaining` 一律拒收）

```yaml
task_id: P0-T01
role: baseline-audit-worker
status: complete|blocked|waiting_human|failed
candidate_sha:          # 工作者不提交，此处可留待控制中心 apply 后回填；工作者报告基于的 baseline_sha
baseline_sha: 6ed243564ddb46f4a46608496d6f05db53c93788
completed: []
modified_files: []
validation_commands: []
validation_results: []
failed_checks: []
remaining_risks: []
issue_ids: []
evidence_paths: []
recommended_next_step: []
requires_escalation: false
context_remaining:      # 必填：百分比或 token 估计
```

## 完成要求（编排器侧，非工作者动作）

- 工作者交回更改后，控制中心 `codex cloud diff/apply` 到 `stream/S0-baseline-audit` 并推送；
- 编排器运行上述验证器 + 完整仓库校验，确认 write_scope 无越界、无机密、无越权改状态；
- 通过后，由**编排器**在 `stream/S0-baseline-audit` 提交
  `evidence/slices/S0/STREAM_COMPLETE.json`，含：候选 SHA、基线 SHA、验证命令与结果、
  证据清单路径、工作者最终 `context_remaining`，以及 `preflight_ok`
  （派遣时温门 `lastWarmUtc` 新鲜度 + 所需上下文包是否就位；无凭据检查）；
- 随后编排器以简体中文、仅指针方式通知监督者（BF-PRE-015）。

## 参考

- 权威任务定义见 `PLAN.md` 中 `P0-T01`。
- 派遣拓扑与温启动规范见 `AGENTS.md`、`docs/planning/MODEL_AND_STREAM_TOPOLOGY.md`（D-026/D-027/D-028）。
- 既存 `cursor/p0-t01-baseline-freeze` 分支为**陈旧、未签发**的参考候选，其 A / C 锚定不可信，
  不得直接采纳；可作对照。
