# P0-T05 工作者契约：运行 S0 集成门并证明并发编排

> 状态：预备草案。派遣时必须由编排轮填入真实 baseline SHA、candidate branch、环境与证据命名空间。

## 元数据

- task_id: `P0-T05`
- role: `qa-security-evidence-gate-worker`
- slice: `S0`
- title: 运行 S0 集成门并证明并发编排
- baseline_sha: `531aa6ffb37517efc6b3c705c040d6d838ab36d0`
- target_branch: `cursor/d043-grok-workers-7c3d`
- model: `cursor-grok-4.5-high` (D-043)
- depends_on: `P0-T02`, `P0-T03`, `P0-T04`
- allowed_write_scope:
  - `evidence/slices/S0/`
  - `evidence/judges/S0/`
  - `BREAK_FIX_LOG.md`
  - `scripts/`
  - `tests/`
  - `harness/schemas/`
- evidence_namespace:
  - `evidence/slices/S0/integrated-gate.json`
  - `evidence/judges/S0/`

## 权威输入

1. `PLAN.md` 中 `P0-T05` 的任务定义。
2. `P0-T02` harness、`P0-T03` validators、`P0-T04` frozen rubric 与 H0 绑定状态。
3. `BREAK_FIX_LOG.md` 当前开放 break/fix 记录。
4. 所有 S0 evidence 与 judge result schema。

## 任务目标

运行 S0 集成门，证明三条互不重叠的 smoke streams 可并发存在且各自内部顺序执行；证明失败任务会派遣 fresh **Grok** worker（D-043）或 bottleneck specialist；执行 saved remediation council 与 fresh judge 验证；证明授权边界 N/N+1（D-044：`authorized_through_phase=8`，phase 9+ 拒绝）。

## 必须满足的验收标准

- 三个 disjoint smoke streams 同时存在时，路径分区、证据命名空间与 mutation owner 不冲突。
- 每条 stream 内部保持顺序执行；不得自我合并。
- 失败任务必须触发 fresh worker 或 bottleneck specialist，并保留可复现失败检查。
- Saved remediation council 只能给出 provisional pass；不得替代 fresh judges。
- Fresh judges 必须在无 saved context 的条件下独立验证。
- 三 judge S0 exit 必须通过，且 judge evidence schema 有效。
- 授权边界：`authorized_through_phase` 内任务可通过；超出相位必须 `phase_unauthorized`（D-044）。

## 必跑验证器

- `harness_smoke`
- `stream_isolation`
- `worker_replacement`
- `bottleneck_dispatch`
- `saved_fresh_council`
- `full_repository`
- `judge_result_schema`

## 证据要求

- 写入或更新 `evidence/slices/S0/integrated-gate.json` 与 `evidence/judges/S0/` 下 judge 证据。
- 若发现 break，必须同步更新 `BREAK_FIX_LOG.md`；不得关闭 stream 或 judge loop 时留下未记录 break。
- 每份证据必须绑定 candidate SHA、验证命令、退出码、时间戳与环境。

## 返回 handoff 模板

```yaml
task_id: P0-T05
role: qa-security-evidence-gate-worker
status: complete|blocked|waiting_human|failed
candidate_sha: ""
baseline_sha: "<由派遣时编排轮填入>"
completed: []
modified_files: []
validation_commands: []
validation_results: []
failed_checks: []
remaining_risks: []
issue_ids: []
evidence_paths: []
recommended_next_step: []
requires_escalation: false
context_remaining: ""
```
