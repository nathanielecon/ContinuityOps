# P0-T03 工作者契约：实现验证器、模型路由、瓶颈派遣与证据适配器

> 状态：ready（依赖 P0-T02 已 verified；D-039 已扩展 write_scope）。派遣时必须由编排轮填入真实 baseline SHA、candidate branch、环境与证据命名空间。勿标 verified。

## 元数据

- task_id: `P0-T03`
- role: `validator-worker`
- slice: `S0`
- title: 实现验证器、模型路由、瓶颈派遣与证据适配器
- baseline_sha: `<ORCH-ROUND-06 治理落地后的 main tip,由派遣时确认>`
- target_branch: `<由派遣时编排轮填入>`
- depends_on: `P0-T02`
- allowed_write_scope:
  - `harness/`
  - `scripts/validators/`
  - `scripts/orchestration/`
  - `scripts/project.mjs`
  - `tests/validators/`
  - `tests/orchestration/`
  - `tests/index.mjs`
  - `tests/package.json`
  - `evidence/schema/`
  - `evidence/slices/S0/validator-contract.json`
- evidence_namespace: `evidence/slices/S0/validator-contract.json`
- scope_note: `D-039 按所有者 issue #28 派遣契约与 CO-010 修复面扩展;不含整个 evidence/slices/ 树。PR #30 在扩展前因 SCOPE-001 失败,须新 fixer/reviewer 轮次,禁止直接重合并。`

## 权威输入

1. `PLAN.md` 中 `P0-T03` 的任务定义。
2. `P0-T02` 交付的 CLI、状态合约与测试基线。
3. 根级 `AGENTS.md` 的语言、权限、证据与 TypeScript 约束。
4. 已接受决策中关于 D-026 至 D-033 的执行拓扑与发布边界。

## 任务目标

实现 fail-closed 的验证器注册与执行框架、SHA 绑定证据适配器、实际模型/模式记录、简体中文工作者通信检查、瓶颈专家派遣配置、Claude proxy 门禁，以及存在 TypeScript 时的稳定 7.x pin 检查。

## 必须满足的验收标准

- 未知 validator ID 必须 fail closed。
- 验证器必须只读；运行验证器不得修改仓库。
- 工作者不得伪造 adapter evidence；证据必须绑定 candidate SHA、命令、退出码、时间与环境。
- 后创建的禁止路径必须被检测。
- 证据 freshness 必须与 SHA 绑定。
- 每轮必须记录实际 model、provider、mode 与 role；不得凭空发明模型可用性。
- 工作者自由文本通信必须为简体中文；机器字段名可保留英文。
- 瓶颈 profile 必须在同一监督会话中派遣，并限制为诊断/交接，不得扩大权限。
- Claude proxy 只能使用 pinned `pxpipe-proxy@0.9.0`，并要求政策门禁、凭据安全、度量与直连 fallback。
- 如仓库出现 TypeScript，必须 pin 稳定 7.x（初始基线 `typescript@7.0.2`）并执行 strict type checking。

## 必跑验证器

- `validator_contract`
- `evidence_schema`
- `secret_scan`
- `forbidden_operations`
- `mutation_isolation`
- `model_routing`
- `worker_language`
- `bottleneck_dispatch`
- `claude_proxy_profile`
- `typescript_7`

## 证据要求

- 写入或更新 `evidence/slices/S0/validator-contract.json`。
- 保留验证器只读证明、未知 ID fail-closed 证明、证据 schema 示例与负向测试输出。
- 证据不得包含 secrets、代理日志中的凭据、用户数据或外部账户材料。

## 返回 handoff 模板

```yaml
task_id: P0-T03
role: validator-worker
status: complete|blocked|waiting_human|failed
candidate_sha: ""
baseline_sha: "<由派遣时编排轮填入>"
completed: []
modified_files: []
validation_commands: []
validation_results: []
failed_checks: []
remaining_risks: []
issue_ids: []
evidence_paths: []
recommended_next_step: []
requires_escalation: false
context_remaining: ""
```
