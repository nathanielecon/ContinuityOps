# P0-T04 工作者契约：冻结 S0 rubrics 并绑定 H0 批准

> 状态：预备草案。派遣时必须由编排轮填入真实 baseline SHA、candidate branch、环境与证据命名空间。此任务涉及 H0 人工门；任何代理不得伪造批准。

## 元数据

- task_id: `P0-T04`
- role: `rubric-setter`
- slice: `S0`
- title: 冻结 S0 rubrics 并绑定 H0 批准
- baseline_sha: `<由派遣时编排轮填入>`
- target_branch: `<由派遣时编排轮填入>`
- depends_on: `P0-T01`, `P0-T03`
- human_gate: `H0`
- allowed_write_scope:
  - `harness/rubrics/`
  - `harness/approvals/`
  - `docs/reviews/rubric-review.md`
- evidence_namespace: `evidence/slices/S0/rubric-freeze.json`

## 权威输入

1. `PLAN.md` 中 `P0-T04` 的任务定义与 H0 门禁。
2. `P0-T01` 分区、上游 pin 与 baseline audit。
3. `P0-T03` 验证器、证据 schema 与模型路由合约。
4. `docs/planning/dispatch/drafts/S0-rubric-draft.zh.md` 可作为提案输入，但不是冻结 rubric。

## 任务目标

为 S0 生成一份冻结、可检查、可哈希绑定的 rubric；记录 plan、execution、validator、rubric、partition bundle hashes；准备 H0 人工批准绑定结构，并验证 rubric mutation policy。

## 必须满足的验收标准

- 每个 slice 只能有一个冻结 rubric；S0 rubric 路径、版本与 hash 必须明确。
- Plan、execution、validator、rubric、partition hashes 必须 pin 到同一 candidate 上下文。
- Rubric mutation policy 必须有正向与负向测试：冻结后未授权修改必须被拒绝或被明确标记为需要重新冻结与重新批准。
- H0 approval binding 只能绑定真实人工批准 receipt；代理不得创建、模拟或声称已获得人工批准。

## 必跑验证器

- `rubric_schema`
- `bundle_hashes`
- `approval_binding`

## 证据要求

- 写入或更新 `evidence/slices/S0/rubric-freeze.json`。
- 记录 frozen rubric hash、bundle hash、approval binding 状态、验证命令、退出码与时间戳。
- 如 H0 尚未由人类批准，handoff 必须为 `waiting_human` 或明确列出 H0 为 remaining risk；不得把草案当作批准。

## 返回 handoff 模板

```yaml
task_id: P0-T04
role: rubric-setter
status: complete|blocked|waiting_human|failed
candidate_sha: ""
baseline_sha: "<由派遣时编排轮填入>"
completed: []
modified_files: []
validation_commands: []
validation_results: []
failed_checks: []
remaining_risks: []
issue_ids: []
evidence_paths: []
recommended_next_step: []
requires_escalation: false
context_remaining: ""
```
