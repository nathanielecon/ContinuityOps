# Claim-safe resume bullets

Aligned to the root README and `docs/claims/matrix.json` (engagement **A3**). Use as-is; do not raise levels.

1. Built ContinuityOps end-to-end contracts and proved a **scoped AWS lab L4** path (apply + lab drill/RTO + teardown) through protected GitHub Actions OIDC to `continuityops-gha` — not production, not Azure live.
2. Published an honest **A3 claims matrix**: lab stack L4, security SBOM L2, agentic workflow L3, upstream integration L2, performance L1, Azure live out — with tip-inherited hosted evidence bound to recorded runs.
3. Designed agentic remediation as **hosted-control** evidence (D-034 patch publisher + D-042 junior actuation intents): proposals and gated GitHub actuation without CursorCloudAgent / in-pod AWS credentials.
4. Evidenced lab incident drill/RTO and staging teardown under OIDC; explicitly non-claimed production drills and ContinuityOps known-good rollback digests.
5. Separated agent construction from cloud authority: short-lived OIDC identities for lab mutation; agent-assisted changes route through protected environment gates rather than default mutation power.
